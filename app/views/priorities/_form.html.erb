<%= simple_form_for @priority, :html => { :class => "form-horizontal" } do |f| %>
  <%= f.error_notification %>

  <%= f.input :title, :input_html => { :class => "span6" } %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
